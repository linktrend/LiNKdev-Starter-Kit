import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Console - Analytics'
};

export default function ConsoleAnalyticsPage() {
  return (
    <div>
      <h1>Console - Analytics (stub)</h1>
    </div>
  );
}
