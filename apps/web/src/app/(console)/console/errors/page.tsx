import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Console - Error Tracking'
};

export default function ConsoleErrorsPage() {
  return (
    <div>
      <h1>Console - Error Tracking (stub)</h1>
    </div>
  );
}
