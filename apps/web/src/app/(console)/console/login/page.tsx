import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Console Login'
};

export default function ConsoleLoginPage() {
  return (
    <div>
      <h1>Console Login (stub)</h1>
    </div>
  );
}
