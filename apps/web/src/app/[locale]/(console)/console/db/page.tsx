import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Console - DB Viewer'
};

export default function ConsoleDbPage() {
  return (
    <div>
      <h1>Console - DB Viewer (stub)</h1>
    </div>
  );
}
