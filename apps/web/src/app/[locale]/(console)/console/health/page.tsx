import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Console - Health'
};

export default function ConsoleHealthPage() {
  return (
    <div>
      <h1>Console - Health (stub)</h1>
    </div>
  );
}
