import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Admin - System Settings'
};

export default function AdminSettingsPage() {
  return (
    <div>
      <h1>Admin - System Settings (stub)</h1>
    </div>
  );
}
