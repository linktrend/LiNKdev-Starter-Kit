import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Admin - User Management'
};

export default function AdminUsersPage() {
  return (
    <div>
      <h1>Admin - User Management (stub)</h1>
    </div>
  );
}
